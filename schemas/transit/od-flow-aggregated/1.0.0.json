{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.blume.systems/transit/od-flow-aggregated/1.0.0.json",
  "title": "OdFlowAggregated",
  "description": "Journey counts between two grid cells over a period. The analytical counterpart to journey-completed: linkage is performed inside transit-core before publication and does not survive into the aggregate. Coarse geo only, and a floor on `journeys` so a flow cannot describe one traveller.",
  "type": "object",
  "properties": {
    "origin_cell": {
      "$ref": "https://schemas.blume.systems/common/geo.json#/$defs/cell_id"
    },
    "destination_cell": {
      "$ref": "https://schemas.blume.systems/common/geo.json#/$defs/cell_id"
    },
    "aggregate": {
      "$ref": "https://schemas.blume.systems/common/aggregate.json",
      "description": "Bucket, identity and revision of this figure."
    },
    "journeys": {
      "type": "integer",
      "minimum": 5,
      "description": "Journeys observed between the two cells. Floored at 5 so an origin-destination pair cannot describe a single traveller."
    },
    "median_duration_s": {
      "type": "integer",
      "minimum": 0,
      "description": "Median journey duration, seconds."
    },
    "mode": {
      "type": "string",
      "enum": [
        "bus",
        "tram",
        "rail",
        "multi"
      ],
      "x-beb-enum": "open",
      "description": "Predominant mode of the flow. Consumers MUST ignore unknown values."
    },
    "provenance": {
      "$ref": "https://schemas.blume.systems/common/provenance.json",
      "description": "How the fact was produced. Required on every event."
    }
  },
  "required": [
    "origin_cell",
    "destination_cell",
    "aggregate",
    "journeys",
    "provenance"
  ],
  "x-beb-aggregate": {
    "window": "PT1H",
    "count_field": "journeys",
    "k_floor": 5
  }
}
