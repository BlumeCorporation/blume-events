{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.blume.systems/vision/footage-sealed/1.0.0.json",
  "title": "FootageSealed",
  "description": "A window of footage was placed under legal hold. Evidential retention, read logged. This is the path a court order takes: it reaches the footage store under a legal basis, not the bus and not the correlation endpoint. May carry a track_ref, which analytical retention may not.",
  "type": "object",
  "properties": {
    "asset_ref": {
      "$ref": "https://schemas.blume.systems/common/asset_ref.json",
      "properties": {
        "class": {
          "enum": [
            "camera"
          ],
          "x-beb-enum": "open"
        }
      },
      "description": "Camera whose footage was sealed."
    },
    "stream_id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]{0,63}$",
      "description": "Registered stream identifier."
    },
    "seal_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$",
      "description": "Seal identifier, referenced by subsequent access logs."
    },
    "track_ref": {
      "$ref": "https://schemas.blume.systems/common/track_ref.json",
      "description": "Track the seal was requested against, where the request named one. Permitted here and banned in analytical retention."
    },
    "window_start": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
      "description": "Start of the sealed window, inclusive."
    },
    "window_end": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
      "description": "End of the sealed window, exclusive."
    },
    "legal_basis_ref": {
      "type": "string",
      "maxLength": 256,
      "description": "Reference to the legal basis for the hold."
    },
    "hold_until": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
      "description": "When the hold expires, if it is time-bounded."
    },
    "digest": {
      "type": "object",
      "description": "Content digest of the sealed material.",
      "properties": {
        "alg": {
          "type": "string",
          "enum": [
            "sha-256",
            "sha-512"
          ],
          "x-beb-enum": "open",
          "description": "Digest algorithm. Consumers MUST ignore unknown values."
        },
        "value": {
          "type": "string",
          "pattern": "^[0-9a-f]{64,128}$",
          "description": "Lowercase hex digest."
        }
      },
      "required": [
        "alg",
        "value"
      ],
      "additionalProperties": false
    },
    "sealed_at": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
      "description": "When the seal was applied."
    },
    "provenance": {
      "$ref": "https://schemas.blume.systems/common/provenance.json",
      "description": "How the fact was produced. Required on every event."
    }
  },
  "required": [
    "asset_ref",
    "stream_id",
    "seal_id",
    "window_start",
    "window_end",
    "legal_basis_ref",
    "sealed_at",
    "provenance"
  ]
}
