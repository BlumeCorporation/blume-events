{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.blume.systems/vision/count-aggregated/1.0.0.json",
  "title": "CountAggregated",
  "description": "Objects of a class counted in a grid cell over a period. The analytical view of vision. Carries no track_ref: an hour-scoped per-person handle in a 90-day store reconstructs exactly what the hour scoping prevents. Coarse geo only, and buckets below the floor are suppressed rather than published.",
  "type": "object",
  "properties": {
    "geo": {
      "$ref": "https://schemas.blume.systems/common/geo.json#/$defs/coarse"
    },
    "object_class": {
      "type": "string",
      "enum": [
        "person",
        "cycle",
        "motorcycle",
        "car",
        "taxi",
        "van",
        "bus",
        "lgv",
        "hgv",
        "unknown"
      ],
      "x-beb-enum": "open",
      "description": "Assigned object class. Consumers MUST ignore unknown values."
    },
    "count": {
      "type": "integer",
      "minimum": 5,
      "description": "Objects counted. Floored at 5; smaller buckets are suppressed, because a count of one in a cell and an hour describes a person."
    },
    "aggregate": {
      "$ref": "https://schemas.blume.systems/common/aggregate.json",
      "description": "Bucket, identity and revision of this figure."
    },
    "asset_ref": {
      "$ref": "https://schemas.blume.systems/common/asset_ref.json",
      "properties": {
        "class": {
          "enum": [
            "camera"
          ],
          "x-beb-enum": "open"
        }
      },
      "description": "Camera the counts were derived from."
    },
    "provenance": {
      "$ref": "https://schemas.blume.systems/common/provenance.json",
      "description": "How the fact was produced. Required on every event."
    }
  },
  "required": [
    "geo",
    "object_class",
    "count",
    "aggregate",
    "provenance"
  ],
  "x-beb-aggregate": {
    "window": "PT1H",
    "count_field": "count",
    "k_floor": 5
  }
}
