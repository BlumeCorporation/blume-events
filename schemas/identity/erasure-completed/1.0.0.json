{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.blume.systems/identity/erasure-completed/1.0.0.json",
  "title": "ErasureCompleted",
  "description": "The data keys for a subject were destroyed. Ciphertext remains in the log and is permanently unrecoverable; systems MUST handle undecryptable payloads as a normal condition rather than an error.",
  "type": "object",
  "properties": {
    "erasure_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$",
      "description": "Erasure being completed. Matches a prior erasure-requested."
    },
    "subject_ref": {
      "$ref": "https://schemas.blume.systems/common/subject_ref.json",
      "description": "Pairwise pseudonym of the data subject. Subject whose data keys were destroyed."
    },
    "completed_at": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
      "description": "When key destruction completed."
    },
    "keys_destroyed": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of per-subject data keys destroyed."
    },
    "attestation": {
      "type": "object",
      "description": "Signed attestation that the data keys were destroyed.",
      "properties": {
        "digest_alg": {
          "type": "string",
          "enum": [
            "sha-256",
            "sha-512"
          ],
          "x-beb-enum": "open",
          "description": "Digest algorithm over the destroyed key set. Consumers MUST ignore unknown values."
        },
        "digest": {
          "type": "string",
          "pattern": "^[0-9a-f]{64,128}$",
          "description": "Lowercase hex digest over the destroyed key identifiers."
        },
        "signer": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9._-]{1,63}$",
          "description": "Key custody principal that signed the attestation."
        }
      },
      "required": [
        "digest_alg",
        "digest",
        "signer"
      ],
      "additionalProperties": false
    },
    "provenance": {
      "$ref": "https://schemas.blume.systems/common/provenance.json",
      "description": "How the fact was produced. Required on every event."
    }
  },
  "required": [
    "erasure_id",
    "subject_ref",
    "completed_at",
    "keys_destroyed",
    "attestation",
    "provenance"
  ]
}
